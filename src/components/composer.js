import React from 'react';

const Composer = () => (
    <div className="composer">
            <div>
              <textarea></textarea>
            </div>
              <button>Send</button>
          </div>
);

export default Composer;
